# 운영 가이드

## 갱신 주기

| 데이터 | 주기 | API 호출 |
|---|---:|---|
| 후보 영상 검색 | 60분 | `search.list` + `videos.list` |
| 기존 후보 통계 | 5분 | `videos.list` |
| 스냅샷 보관 | 영상당 최근 288개 | 브라우저 Local Storage |

## 점수 해석

- **5분 실측**: 직전 브라우저 스냅샷과 최신 조회수 차이를 시간당 값으로 환산합니다.
- **게시 후 평균 추정**: 첫 방문처럼 직전 스냅샷이 없을 때 누적 조회수를 게시 후 경과 시간으로 나눕니다.
- 점수는 후보 집합 내 상대적 속도를 사용하므로 서로 다른 검색 조건의 점수를 직접 비교하면 안 됩니다.

## 24시간 무인 수집으로 확장할 때

현재 버전은 API 키 노출을 피하기 위해 사용자의 브라우저에서만 호출합니다. 페이지가 닫혀 있어도 계속 수집하려면 다음 구성이 필요합니다.

1. 서버리스 예약 작업(5분 통계, 1시간 후보 검색)
2. 비공개 서버 환경 변수의 YouTube API 키
3. 스냅샷 저장용 데이터베이스
4. 공개 읽기 API와 요청 제한
5. YouTube API 할당량 모니터링

API 키나 호스팅 계정이 제공되면 이 구조로 확장할 수 있습니다.

## 장애 확인

- `quotaExceeded`: Google Cloud의 일일 YouTube API 할당량 확인
- `keyInvalid`: 키 오탈자와 API 활성화 여부 확인
- `ipRefererBlocked`: 배포 URL이 API 키 HTTP 리퍼러 허용 목록에 있는지 확인
- 검색 결과 없음: 기간을 7일 또는 30일로 늘리거나 키워드를 비워 재검색
